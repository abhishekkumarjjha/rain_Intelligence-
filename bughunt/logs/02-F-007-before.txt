# F-007 BEFORE — the new regression section against the pre-fix themes endpoint.

── regression — two scopes read at once, both survive on disk
  ok  both concurrent scope reads succeed
  FAIL both scopes are on disk — neither write clobbered the other
       "all" was lost from disk; found ["checking"]
  FAIL a lost scope is a re-billed scope — the saved read is what a reopen serves
       all would be re-read, and re-billed, on the next open

106 passed, 2 failed

Failures:
  · both scopes are on disk — neither write clobbered the other: "all" was lost from disk; found ["checking"]
  · a lost scope is a re-billed scope — the saved read is what a reopen serves: all would be re-read, and re-billed, on the next open
